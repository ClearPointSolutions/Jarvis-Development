{
  "name": "jarvis-phase-03-full-stack-acceptance",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "python -m unittest discover -s tests -p test_*.py && node --test frontend/validation.test.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}

# Jarvis Phase 0 acceptance project

This deliberately small Python package is copied to a disposable directory and
committed before a real acceptance run. It is not Jarvis and contains no secrets.

Verification: `python -m pytest -q` after installing the package in editable
mode with its `test` extra.
